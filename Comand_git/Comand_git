git status — перевірка стану

git add . — додаємо нові/змінені файли

git commit -m "текст" — зберігаємо коміт

git push — відправляємо на GitHub

(іноді) git pull — підтягнути зміни з GitHub

git log --oneline Швидко подивитися історію останніх комітів (коротко).

git diff Показує, що саме ти змінив у коді з моменту останнього збереження.

git checkout -b назва_гілки  Створює нову гілку.

git commit --amend -m "новий текст" Якщо зробив коміт, але помітив друкарську помилку в 
повідомленні або забув додати файл.